<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aimcc.blog.mapper.TagMapper">

    <!-- 标签云：每个标签 + 实时文章数（空标签也显示，计 0 篇） -->
    <select id="selectTagCloud" resultType="com.aimcc.blog.dto.TagVO">
        select t.id,
               t.name,
               count(at.article_id) as article_count
        from tag t
                 left join article_tag at on t.id = at.tag_id
        group by t.id, t.name
        order by article_count desc
    </select>

    <!-- 批量查若干文章的标签连线（行形状：文章id + 标签id + 标签名，内部组装用） -->
    <select id="selectTagsByArticleIds" resultType="com.aimcc.blog.dto.ArticleTagRow">
        select at.article_id,
               t.id   as tag_id,
               t.name as tag_name
        from article_tag at
                 left join tag t on at.tag_id = t.id
        where at.article_id in
        <foreach collection="articleIds" item="aid" open="(" separator="," close=")">
            #{aid}
        </foreach>
        order by at.article_id
    </select>

</mapper>