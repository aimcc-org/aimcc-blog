# 插件

Markdown/rehype/remark/Expressive Code 插件统一放在这里。

- `markdown/remark/`: Markdown AST 阶段
- `markdown/rehype/`: HTML AST 阶段
- `expressive-code/`: 代码块扩展

不要把 Markdown pipeline 插件散落到 route/template 中。
